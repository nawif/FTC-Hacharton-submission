<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="android.com.merath">

    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="false"
        android:theme="@style/AppTheme">
        <activity android:name="com.alsahirAndroid.merth.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.alsahirAndroid.merth.MenuActivity" />
        <activity android:name="com.alsahirAndroid.merth.FavoriteActivity" />
        <activity android:name="com.alsahirAndroid.merth.AlertActivity" />
        <activity android:name="com.alsahirAndroid.merth.AboutActivity" />
        <activity android:name="com.alsahirAndroid.merth.ReaderActivity" />
        <activity android:name="com.alsahirAndroid.merth.AddAlertActivity"></activity>

        <receiver
            android:name="com.alsahirAndroid.merth.NotificationReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.REBOOT"/>
            </intent-filter>
        </receiver>

    </application>

</manifest>